{
  "name": "spring_body_lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests",
    "coverage": "node --test --experimental-test-coverage tests",
    "check": "npm run coverage && npm run build"
  }
}
